name: CI

on:
  push:
    branches:
      - main
  pulll_request:
    branches:[main]

jobs:
  build_docker:
    env:
      MAIL_USERNAME: ${{ secrets.MAIL_USERNAME }}
      MAIL_PASSWORD: ${{ secrets.MAIL_PASSWORD }}
      
    steps:
      - name: Checkout
        uses: actions/checkout@v3

    - name: Echo Hello
      run: |
        echo Hello World